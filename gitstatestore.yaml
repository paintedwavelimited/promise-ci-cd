apiVersion: platform.kratix.io/v1alpha1
kind: GitStateStore
metadata:
  name: default
spec:
  url: https://github.com/paintedwavelimited/promise-ci-cd
  branch: main
  secretRef:
    name: git-credentials
    namespace: kratix-platform-system
